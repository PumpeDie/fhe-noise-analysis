services:
  pandoc:
    image: pandoc/extra:latest
    volumes:
      - ./:/data:z
    entrypoint: ["/bin/sh", "-c"]
    command:
      - |
        pandoc --pdf-engine=lualatex \
        --syntax-highlighting=tango \
        --metadata-file=/data/metadata.yaml \
        --resource-path=/data/res \
        /data/*.md \
        -o /data/RapportCrypto.pdf